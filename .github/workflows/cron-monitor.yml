name: Cron Monitor

on:
  schedule:
    - cron: '0 * * * *'  # every hour
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest
    timeout-seconds: 60
    steps:
      - name: Health check
        run: |
          # Placeholder: actual monitoring happens via OpenClaw cron jobs
          # This workflow exists as a GitHub-side backup health check
          echo "Cron monitor tick: $(date)"
          echo "Active monitoring: Lambda Watchdog (9d10560b) every 5min via OpenClaw"
          echo "Self-healing: self_heal.sh runs hourly via OpenClaw cron"
